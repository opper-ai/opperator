# Opperator

## Quickstart

### Installing and running Opperator

You can install Opperator by running the following command in your terminal:

```bash
curl -fsSL http://opper.ai/opperator-install | bash
```

Then simply run `op` or `opperator` to get started:

```shell
opperator
```

